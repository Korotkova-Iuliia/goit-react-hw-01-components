export const Profile = ({ username }) => {
  console.log(username);
  //   return (
  //     <div>
  //       {user.map(data => (
  //         <User key={data.username} data={data} />
  //       ))}
  //     </div>
  //   );
};
